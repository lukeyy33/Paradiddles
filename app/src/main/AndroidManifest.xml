<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lukewaugh.paradiddles">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter tools:ignore="GoogleAppIndexingWarning">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Splash Activity -->
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Singles Activity -->
        <activity
            android:name=".SinglesActivity"
            android:label="@string/singlesActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Doubles Activity -->
        <activity
            android:name=".DoublesActivity"
            android:label="@string/doublesActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Triplets Activity -->
        <activity
            android:name=".TripletsActivity"
            android:label="@string/triplesActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Drags Activity -->
        <activity
            android:name=".DragsActivity"
            android:label="@string/dragsActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Rolls Activity -->
        <activity
            android:name=".RollsActivity"
            android:label="@string/rollsActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Flams Activity -->
        <activity
            android:name=".FlamsActivity"
            android:label="@string/flamsActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Login Activity -->
        <activity
            android:name=".authClasses.LoginActivity"
            android:label="@string/loginActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- Account Activity -->
        <activity
            android:name=".authClasses.AccountActivity"
            android:label="Account Activity"
            android:theme="@style/AppTheme"></activity>
        <!-- Register Activity -->
        <activity
            android:name=".authClasses.RegisterActivity"
            android:label="Register Activity"
            android:theme="@style/AppTheme"></activity>

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settingsActivityTitle"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <!--
            ATTENTION: This was auto-generated to add Google Play services to your project for
            App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>